# Base URL of the existing Apex Academy Render backend (Express + Postgres + JWT).
# The GCSE frontend reads this at build time. Leave it unset to run the app in
# offline demo mode, where auth and progress persist to the browser's localStorage.
VITE_API_URL=https://your-apex-backend.onrender.com
